# Render.com 프론트엔드 서비스 설정
# 이 파일은 ui 폴더에 위치합니다.

services:
  - type: web
    name: order-app-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    # 환경 변수는 Render 대시보드에서 설정

